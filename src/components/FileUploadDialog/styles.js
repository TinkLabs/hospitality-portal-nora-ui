const styles = theme => {
    return ({
      dialogContent: {
        textAlign: 'center',
      },
      fileInput: {
        display: 'none',
      },
      fileInputLabel: {
        cursor: 'pointer',
      },
      imageContainer: {
        width: '100%',
        height: '100%',
      }
    });
}

export default styles;